# keyboard-ui

React + Vite + TypeScript frontend for 7Panel Studio.

Connects to the Python backend at `http://localhost:5000`.

## Dev

```bash
npm install
npm run dev
```

Opens at `http://localhost:5173`.
